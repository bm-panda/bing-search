# 必应搜索

## 简介

选中复制任意文本，**双击 Ctrl+C**，直接唤起默认浏览器，自动调用必应搜索，关键词自动带入搜索框。支持中文、英文、各类特殊符号，自动完成 URL 编码，无需手动粘贴复制内容，查询一步到位。

## 运行环境

脚本核心依赖 PowerShell 7及以上版本。推荐使用【不忙脚本盒子】运行，盒子自动处理环境依赖，原生支持快捷键、右键菜单、定时任务等多种触发方式，开箱即用，免去手动配置的麻烦。

## 安装方法

1. 打开**不忙脚本盒子**
2. 进入「脚本市场」
3. 搜索「**必应搜索**」
4. 点击「安装」即可使用

## 使用方法

1. 鼠标选中需要查询的文字，按下 `Ctrl+C+C` 复制
2. 自动唤起浏览器，直接在必应执行搜索

---
